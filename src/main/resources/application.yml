spring:
  application:
    name: rocket-cart-api
  datasource:
    url: jdbc:postgresql://localhost:5432/rocket_cart
    username: user
    password: rocket123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always
